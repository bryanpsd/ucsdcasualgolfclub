import { style } from '@vanilla-extract/css';
import { recipe } from '@vanilla-extract/recipes';
import { sprinkles } from '~styles/sprinkles.css';

export const nav = style([sprinkles({ display: 'flex' })]);

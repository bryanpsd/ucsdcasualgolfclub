---
export const prerender = true

import { ResponsiveHeadline } from '~components/ResponsiveHeadline'
import ErrorLayout from '~layouts/Error/Error.astro'
---

<ErrorLayout title="Not Found">
  <ResponsiveHeadline size={2} as="h1">Oops! We can't find that page.</ResponsiveHeadline>
</ErrorLayout>
